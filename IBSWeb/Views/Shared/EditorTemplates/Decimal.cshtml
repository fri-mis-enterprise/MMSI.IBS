@model decimal?
<input asp-for="@Model" class="form-control" type="number" step="0.0001" value="@(Model.HasValue ? Model.Value.ToString("0.####") : "")" style="border: 1px solid var(--outline-variant); border-radius: var(--radius-xl);" />
