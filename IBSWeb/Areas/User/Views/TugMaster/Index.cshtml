@{
    ViewData["Title"] = "Tugboat Masters";
    ViewData["IsFullWidth"] = true;
}

<div class="modern-layout">
    <main class="modern-container">
        <!-- Header Section -->
        <div class="flex-stack">
            <div>
                <div class="flex items-center gap-2 mb-1">
                    <span class="modern-label" style="color: var(--on-surface-variant)">MSAP</span>
                    <span class="material-symbols-outlined text-xs">chevron_right</span>
                    <span class="modern-label" style="color: var(--on-surface)">Tug Masters</span>
                </div>
                <h1 class="modern-headline-lg">@ViewData["Title"]</h1>
            </div>
            <div class="flex gap-2">
                <a asp-action="Create" class="modern-btn-primary">
                    <span class="material-symbols-outlined">add_circle</span>
                    CREATE NEW
                </a>
            </div>
        </div>

        <!-- Table Card -->
        <div class="modern-card">
            <div class="modern-card-header-action flex justify-between items-center w-full">
                <div class="flex items-center gap-2">
                    <span class="material-symbols-outlined text-primary">person_check</span>
                    <h2 class="modern-headline-md">Master List</h2>
                </div>
            </div>
            <div class="modern-card-body">
                <table class="modern-table" id="tugMastersTable">
                    <thead>
                        <tr>
                            <th>Master #</th>
                            <th>Master Name</th>
                            <th class="text-center">Active?</th>
                            <th class="text-center">Actions</th>
                        </tr>
                    </thead>
                </table>
            </div>
        </div>
    </main>
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            const editUrl = '@Url.Action("Edit", "TugMaster", new { area = "User" })';
            const deleteUrl = '@Url.Action("Delete", "TugMaster", new { area = "User" })';

            let table = $('#tugMastersTable').DataTable(ModernTable.config({
                serverSide: false,
                ajax: ModernTable.ajax("@Url.Action("GetTugMasterList", "TugMaster", new { area = "User" })"),
                columns: [
                    { data: "tugMasterNumber" },
                    { 
                        data: "tugMasterName",
                        render: (data) => `<span class="font-bold uppercase">${data}</span>`
                    },
                    { 
                        data: "isActive",
                        className: "text-center",
                        render: (data) => data ? ModernTable.render.badge('YES', 'bg-success text-white') : ModernTable.render.badge('NO', 'bg-secondary text-white')
                    },
                    {
                        data: null,
                        orderable: false,
                        className: "text-center",
                        render: (data, type, row) => ModernTable.render.dropdown([
                            { label: 'Edit Master', icon: 'edit', url: `${editUrl}?id=${row.tugMasterId}` },
                            { 
                                label: 'Delete Master', 
                                icon: 'delete', 
                                isError: true,
                                onclick: `confirmDelete('${deleteUrl}?id=${row.tugMasterId}')`
                            }
                        ])
                    }
                ]
            }));
        });

        function confirmDelete(url) {
            ModernAlert.confirm({
                title: 'Delete Tug Master?',
                text: "Are you sure you want to delete this tug master entry?",
                icon: 'warning',
                confirmText: 'Yes, Delete',
                cancelText: 'Cancel'
            }).then((result) => {
                if (result.isConfirmed) {
                    window.location.href = url;
                }
            });
        }
    </script>
}
