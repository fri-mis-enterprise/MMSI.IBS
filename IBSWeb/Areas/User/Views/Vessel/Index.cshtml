@{
    ViewData["Title"] = "Vessels";
    ViewData["IsFullWidth"] = true;
}

<div class="modern-layout">
    <main class="modern-container">
        <!-- Header Section -->
        <div class="flex-stack">
            <div>
                <div class="flex items-center gap-2 mb-1">
                    <span class="modern-label" style="color: var(--on-surface-variant)">MSAP</span>
                    <span class="material-symbols-outlined text-xs">chevron_right</span>
                    <span class="modern-label" style="color: var(--on-surface)">Vessels</span>
                </div>
                <h1 class="modern-headline-lg">@ViewData["Title"]</h1>
            </div>
            <div class="flex gap-2">
                <a asp-action="Create" class="modern-btn-primary">
                    <span class="material-symbols-outlined">add_circle</span>
                    CREATE NEW
                </a>
            </div>
        </div>

        <!-- Table Card -->
        <div class="modern-card">
            <div class="modern-card-header-action flex justify-between items-center w-full">
                <div class="flex items-center gap-2">
                    <span class="material-symbols-outlined text-primary">directions_boat</span>
                    <h2 class="modern-headline-md">Vessel List</h2>
                </div>
            </div>
            <div class="modern-card-body">
                <table class="modern-table" id="vesselsTable">
                    <thead>
                        <tr>
                            <th>Vessel #</th>
                            <th>Vessel Name</th>
                            <th>Vessel Type</th>
                            <th class="text-center">Actions</th>
                        </tr>
                    </thead>
                </table>
            </div>
        </div>
    </main>
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            const editUrl = '@Url.Action("Edit", "Vessel", new { area = "User" })';
            const deleteUrl = '@Url.Action("Delete", "Vessel", new { area = "User" })';

            let table = $('#vesselsTable').DataTable(ModernTable.config({
                serverSide: false,
                ajax: ModernTable.ajax("@Url.Action("GetVesselList", "Vessel", new { area = "User" })"),
                columns: [
                    { data: "vesselNumber" },
                    { 
                        data: "vesselName",
                        render: (data) => `<span class="font-bold uppercase">${data}</span>`
                    },
                    { 
                        data: "vesselType",
                        render: (data) => ModernTable.render.badge(data || 'N/A', 'bg-info text-white')
                    },
                    {
                        data: null,
                        orderable: false,
                        className: "text-center",
                        render: (data, type, row) => ModernTable.render.dropdown([
                            { label: 'Edit Vessel', icon: 'edit', url: `${editUrl}?id=${row.vesselId}` },
                            { 
                                label: 'Delete Vessel', 
                                icon: 'delete', 
                                isError: true,
                                onclick: `confirmDelete('${deleteUrl}?id=${row.vesselId}')`
                            }
                        ])
                    }
                ]
            }));
        });

        function confirmDelete(url) {
            ModernAlert.confirm({
                title: 'Delete Vessel?',
                text: "Are you sure you want to delete this vessel entry?",
                icon: 'warning',
                confirmText: 'Yes, Delete',
                cancelText: 'Cancel'
            }).then((result) => {
                if (result.isConfirmed) {
                    window.location.href = url;
                }
            });
        }
    </script>
}
