@model IBS.Models.MSAP.MasterFile.Terminal

@{
    ViewData["Title"] = "Edit Terminal";
    ViewData["IsFullWidth"] = true;
}

<div class="modern-layout">
    <form asp-action="Edit" method="post" id="mainForm">
        <input type="hidden" asp-for="TerminalId" />
        <main class="modern-container">
            <!-- Header Section -->
            <div class="flex-stack">
                <div>
                    <div class="flex items-center gap-2 mb-1">
                        <span class="modern-label">MSAP</span>
                        <span class="material-symbols-outlined text-xs">chevron_right</span>
                        <a class="modern-label" style="text-decoration:none;" asp-action="Index">Terminals</a>
                        <span class="material-symbols-outlined text-xs">chevron_right</span>
                        <span class="modern-label" style="color: var(--on-surface)">Edit Terminal</span>
                    </div>
                    <h1 class="modern-headline-lg">@ViewData["Title"]</h1>
                </div>
                <div class="flex gap-2">
                    <button type="submit" class="modern-btn-primary">
                        <span class="material-symbols-outlined">save</span> Save Changes
                    </button>
                    <a asp-action="Index" class="modern-btn-secondary">
                        <span class="material-symbols-outlined">cancel</span> Cancel
                    </a>
                </div>
            </div>

            <div class="modern-grid justify-center">
                <section class="modern-col-6">
                    <div class="modern-card">
                        <div class="modern-card-header-info">
                            <span class="material-symbols-outlined">edit</span>
                            <h2 class="modern-headline-md">Terminal Details</h2>
                        </div>
                        <div class="modern-card-body">
                            <div asp-validation-summary="ModelOnly" class="text-error mb-2"></div>
                            
                            <div class="flex flex-column gap-4">
                                <div>
                                    <label asp-for="TerminalNumber" class="modern-label">Terminal Number <span class="text-error">*</span></label>
                                    <input asp-for="TerminalNumber" class="form-control" style="border: 1px solid var(--outline-variant); border-radius: var(--radius-xl);" placeholder="Enter 3-digit code" maxlength="3" />
                                    <span asp-validation-for="TerminalNumber" class="text-error text-xs"></span>
                                </div>

                                <div>
                                    <label asp-for="TerminalName" class="modern-label">Terminal Name <span class="text-error">*</span></label>
                                    <input asp-for="TerminalName" class="form-control" style="border: 1px solid var(--outline-variant); border-radius: var(--radius-xl);" placeholder="Enter terminal name" />
                                    <span asp-validation-for="TerminalName" class="text-error text-xs"></span>
                                </div>

                                <div>
                                    <label asp-for="PortId" class="modern-label">Linked Port <span class="text-error">*</span></label>
                                    <select asp-for="PortId" asp-items="@Model.Ports" class="js-modern-select" data-placeholder="-- Select Port --">
                                        <option value="">-- Select Port --</option>
                                    </select>
                                    <span asp-validation-for="PortId" class="text-error text-xs"></span>
                                </div>
                            </div>
                        </div>
                    </div>
                </section>
            </div>
        </main>
    </form>
</div>

@section Scripts {
    }
